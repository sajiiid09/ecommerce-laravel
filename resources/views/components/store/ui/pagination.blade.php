@props(['current' => 1, 'total' => 1])
@if ($total > 1)<nav class="mt-6 flex items-center justify-center gap-2" aria-label="Pagination">@for ($page = 1; $page <= $total; $page++)<a href="{{ request()->fullUrlWithQuery(['page' => $page]) }}" @class(['grid size-10 place-items-center rounded-control border text-sm font-semibold', 'border-store-blue bg-store-blue text-white' => $page === $current, 'border-store-border bg-white text-store-text hover:border-store-blue hover:text-store-blue' => $page !== $current]) aria-current="{{ $page === $current ? 'page' : 'false' }}">{{ $page }}</a>@endfor</nav>@endif
