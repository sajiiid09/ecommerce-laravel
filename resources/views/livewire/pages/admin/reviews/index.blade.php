<div class="p-5 sm:p-8"><div class="mx-auto max-w-[1200px]"><div><p class="text-sm text-slate-500">StoreZ / Reviews</p><h1 class="mt-1 text-3xl font-extrabold tracking-tight text-slate-900">Product Reviews</h1><p class="mt-1 text-sm text-slate-500">Moderate customer feedback before it appears publicly.</p></div><div class="mt-6 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><select wire:model.live="status" class="h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option><option value="">All reviews</option></select></div><div class="mt-5 space-y-4">@forelse($reviews as $review)<article wire:key="review-{{ $review->id }}" class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="font-bold text-slate-900">{{ $review->product?->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $review->name }} · {{ $review->created_at->format('M j, Y') }} · {{ str_repeat('★', $review->rating) }}</p></div><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold capitalize text-slate-600">{{ $review->status }}</span></div>@if($review->title)<h2 class="mt-4 font-bold text-slate-900">{{ $review->title }}</h2>@endif<p class="mt-2 text-sm leading-6 text-slate-600">{{ $review->review }}</p>@if($review->is_verified_purchase)<p class="mt-3 text-xs font-bold text-emerald-600">Verified purchase</p>@endif<div class="mt-4 flex flex-wrap gap-2">@if($review->status !== 'approved')<button wire:click="approve({{ $review->id }})" class="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white">Approve</button>@endif @if($review->status !== 'rejected')<button wire:click="reject({{ $review->id }})" class="rounded-lg border border-amber-200 px-3 py-2 text-xs font-bold text-amber-700">Reject</button>@endif<button wire:click="delete({{ $review->id }})" class="rounded-lg border border-red-200 px-3 py-2 text-xs font-bold text-red-600">Delete</button></div></article>@empty<div class="rounded-xl border border-slate-200 bg-white p-12 text-center text-sm text-slate-500">No reviews in this state.</div>@endforelse</div><div class="mt-5">{{ $reviews->links() }}</div></div></div>
