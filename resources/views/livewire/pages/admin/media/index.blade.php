<div class="min-h-screen bg-slate-50 p-8 text-slate-900"><div class="mx-auto max-w-7xl"><h1 class="mb-6 text-2xl font-semibold">Media Library</h1><form wire:submit="upload" class="mb-6 flex gap-3 rounded-xl border border-slate-200 bg-white p-5"><input type="file" wire:model="file" accept="image/*"><button class="rounded-lg bg-indigo-600 px-4 py-2 text-white">Upload image</button></form><div class="grid grid-cols-2 gap-4 sm:grid-cols-4 lg:grid-cols-6">@forelse($assets as $asset)<div class="overflow-hidden rounded-xl border border-slate-200 bg-white"><img src="{{ $asset->url() }}" alt="{{ $asset->filename }}" class="aspect-square w-full object-cover"><p class="truncate p-2 text-xs">{{ $asset->filename }}</p></div>@empty<p class="text-slate-500">No media assets yet.</p>@endforelse</div><div class="mt-5">{{ $assets->links() }}</div></div></div>
