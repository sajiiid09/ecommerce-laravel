<div class="min-h-screen bg-slate-50 p-8 text-slate-900"><div class="mx-auto max-w-6xl"><a href="{{ url('/admin/catalog/products') }}" class="text-sm text-indigo-600">← Products</a><div class="my-4 flex items-center justify-between"><h1 class="text-2xl font-semibold">{{ $product->name }} — Options & Variants</h1><button wire:click="generate" class="rounded-lg bg-indigo-600 px-4 py-2 text-white">Generate Variants</button></div><section class="mb-6 rounded-xl border border-slate-200 bg-white p-6"><h2 class="mb-4 font-semibold">Product Options</h2><form wire:submit="addOption" class="mb-4 flex gap-2"><x-ui.input wire:model="optionName" placeholder="Option name, e.g. Color" class="min-w-0 flex-1 !rounded-lg" controlClass="!rounded-lg !border-slate-300 !bg-white" /><button class="rounded-lg bg-slate-900 px-3 py-2 text-sm text-white">Add option</button></form><div class="grid gap-3 md:grid-cols-2">@forelse($options as $option)<div class="rounded-lg border border-slate-200 p-4"><div class="font-medium">{{ $option->name }}</div><div class="my-3 flex flex-wrap gap-2">@foreach($option->values as $value)<span class="rounded-full bg-slate-100 px-3 py-1 text-sm">{{ $value->value }}</span>@endforeach</div><form wire:submit="addValue({{ $option->id }})" class="flex gap-2"><x-ui.input wire:model="optionValue" placeholder="Add value" class="min-w-0 flex-1 !rounded-lg" controlClass="!rounded-lg !border-slate-300 !bg-white" /><button class="rounded-lg border border-slate-300 px-3 py-1 text-sm">Add</button></form></div>@empty<p class="text-sm text-slate-500">Add options such as Color, Size, or Storage to generate variants.</p>@endforelse</div></section><section class="overflow-hidden rounded-xl border border-slate-200 bg-white"><div class="border-b border-slate-200 p-4"><h2 class="font-semibold">Generated Variants</h2></div><table class="min-w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="px-4 py-3">Variant</th><th class="px-4 py-3">SKU</th><th class="px-4 py-3">Price</th><th class="px-4 py-3">Available</th><th class="px-4 py-3">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($variants as $variant)<tr><td class="px-4 py-3">{{ $variant->name ?: 'Default' }}</td><td class="px-4 py-3">{{ $variant->sku }}</td><td class="px-4 py-3">{{ $variant->regular_price_minor }}</td><td class="px-4 py-3">{{ $variant->availableQuantity() }}</td><td class="px-4 py-3">{{ $variant->is_active ? 'Active' : 'Inactive' }}</td><td class="px-4 py-3"><button wire:click="toggle({{ $variant->id }})" class="text-indigo-600">Toggle</button></td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-slate-500">No variants yet. Add options and generate combinations.</td></tr>@endforelse</tbody></table></section></div></div>
